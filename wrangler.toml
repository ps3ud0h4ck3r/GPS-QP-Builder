name = "gps-qp-builder"
main = "worker/index.js"
compatibility_date = "2026-09-12"

[assets]
directory = "./dist"
binding = "ASSETS"
run_worker_first = ["/api/*"]

# The D1 binding is intentionally managed in Cloudflare until the database ID
# is added to this source-controlled Wrangler configuration.
